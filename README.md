# NoTemp Remake
## Description
NoTemp Remake is a simple program that cleans junk files on Windows and it doesn't need administrator privileges and an installation to work

## Arguments
You can use ```/y``` to automatically accept every prompt

Example:
```NoTemp.exe /y```
